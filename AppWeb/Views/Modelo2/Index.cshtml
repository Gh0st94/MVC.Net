@model IEnumerable<AppWeb.Models.Modelo2>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cargo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cedula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cargo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cedula)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Modelo2Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Modelo2Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Modelo2Id })
        </td>
    </tr>
}

</table>
