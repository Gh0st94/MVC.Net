@model IEnumerable<AppWeb.Models.Modelo1>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Modelo2.Modelo2Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cargo)
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Modelo2.Modelo2Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cargo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Modelo1Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Modelo1Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Modelo1Id })
        </td>
    </tr>
    }

    <tr>
        <th>Suma Total Cargo</th>
        <th></th>
        <th>@ViewBag.Suma</th>
    </tr>

</table>
